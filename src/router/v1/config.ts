import type { TypeRouter } from '@/types/types';

export const v1: TypeRouter = {
  dashboard: '/dashboard',
  users: '/users',
  products: '/products',
  orders: '/orders',
  categories: '/categories',
  settings: '/settings'
};
